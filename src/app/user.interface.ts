export interface User {
  id: number;
  user: {
    name: string;
    email: string;
    username: string;
    password: string;
    
  };
  message: string;
  // Otras propiedades del usuario aquí
}
